{
    "name": "profit-loss-monorepo",
    "private": true,
    "packageManager": "npm@10.9.2",
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build",
        "lint": "turbo run lint",
        "clean": "turbo run clean && rm -rf node_modules && npm install"
    },
    "devDependencies": {
        "turbo": "latest"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}